---
import MainLayout from "../layouts/MainLayout.astro";
import Button from '../components/Button.astro';
---
<MainLayout title="Contact" description="Contact Exceptive for high-speed maritime training, custom courses, and accommodation inquiries. Reach out to book your experience or ask a question.">
   <div class="fixed inset-0 z-[-1] bg-[url('/bg_blue.jpg')] bg-repeat bg-center bg-cover"></div>
   <div id="grid-bg" class="fixed inset-0 z-[-1] pointer-events-none">
  <img src="/bg_grid.svg" class="w-full h-full object-cover transition-transform duration-75" />
  </div>

 <section class="text-white min-h-screen 2xl:pt-4xl px-sm md:px-m lg:px-lg">
 <article class="pt-4xl grid md:grid-cols-2">
        <div class="">
            <h2 class="font-black text-h2Sm md:text-h2Md lg:text-h2 text-focus-in">Get <br> <span class="font-regular">in touch</span></h2>
            <p class="font-black text-bodySm md:text-body pt-s text-focus-in delayed">Whether you're looking to strengthen your crew's high-speed navigation skills, explore tailored maritime training, or plan a unique stay on Aspö with the option of guided boat experiences - we're here to help. <br><br> Reach out to learn more about our specialized courses, availability, or how we can support your maritime goals with precision, experience, and action.</p>
        </div>
      <!--   form  -->
    
     <div class="">
  <form 
    name="contact" 
    method="POST"
    data-netlify="true" 
    action="/thankyou"
    class="font-orbitron text-bodyDeco text-white p-6 grid gap-4 grid-cols-1 md:grid-cols-2 max-w-4xl mx-auto"
  >
    <!-- Steg 2: döljer input med formnamnet -->
    <input type="hidden" name="form-name" value="contact" />

    <div class="col-span-2">
      <label class="block text-sm font-bold mb-1">Full name</label>
      <input type="text" name="fullname" class="w-full border border-white bg-transparent p-2 text-white placeholder-white" placeholder="Your name" required />
    </div>

    <div>
      <label class="block text-sm font-bold mb-1">E-mail</label>
      <input type="email" name="email" class="w-full border border-white bg-transparent p-2 text-white placeholder-white" placeholder="you@example.com" required />
    </div>

    <div>
      <label class="block text-sm font-bold mb-1">Phone</label>
      <input type="tel" name="phone" class="w-full border border-white bg-transparent p-2 text-white placeholder-white" placeholder="+46 700 000 000" />
    </div>

    <div>
      <label class="block text-sm font-bold mb-1">Select profile</label>
      <select name="profile" class="w-full border border-white bg-transparent p-2 text-white" required>
        <option value="" class="bg-blue-500" disabled selected>Choose...</option>
        <option value="Government" class="bg-blue-500">Government</option>
        <option value="Commercial" class="bg-blue-500">Commercial</option>
        <option value="Press" class="bg-blue-500">Press</option>
        <option value="Private" class="bg-blue-500">Private</option>
        <option value="Supplier" class="bg-blue-500">Supplier</option>
      </select>
    </div>

    <div>
      <label class="block text-sm font-bold mb-1">Country</label>
      <select name="country" class="w-full border border-white bg-transparent p-2 text-white" required>
        <option value="" disabled selected>Choose country...</option>
        <option value="Sweden" class="bg-blue-500">Sweden</option>
        <option value="Norway" class="bg-blue-500">Norway</option>
        <option value="Denmark" class="bg-blue-500">Denmark</option>
        <option value="United Arab Emirates" class="bg-blue-500">United Arab Emirates</option>
        <option value="United Kingdom" class="bg-blue-500 hover:bg-blue-50">United Kingdom</option>
      </select>
    </div>

    <div class="col-span-2">
      <label class="block text-sm font-bold mb-1">Tell us more</label>
      <textarea name="message" class="w-full border border-white bg-transparent p-2 text-white placeholder-white" rows="4" placeholder="Your message..."></textarea>
    </div>

    <div class="col-span-2 flex justify-end mt-2">
      <Button variant="primary" size="medium" type="submit">Send</Button>
    </div>
  </form>
    </div>
     </article>
     </section>
</MainLayout>

<script>
  const grid = document.getElementById('grid-bg');
  let targetX = 0;
  let targetY = 0;
  let currentX = 0;
  let currentY = 0;


  document.addEventListener('mousemove', (e) => {
    const ratioX = (e.clientX / window.innerWidth - 0.5);
    const ratioY = (e.clientY / window.innerHeight - 0.5);


    targetX = ratioX * 30; 
    targetY = ratioY * 30;
  });


  function animateGrid() {

    currentX += (targetX - currentX) * 0.08;
    currentY += (targetY - currentY) * 0.08;

    grid.style.transform = `translate(${currentX}px, ${currentY}px)`;

    requestAnimationFrame(animateGrid);
  }

  animateGrid();
</script>